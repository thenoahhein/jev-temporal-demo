# Required for real Jev calls (leave unset to use the mock)
TYPESAFE_API_KEY=

# Optional: real reasoning-model consultation (gpt-4o-mini)
OPENAI_API_KEY=

# Set to 1 to force the heuristic Jev stand-in even when TYPESAFE_API_KEY is set
JEV_MOCK=
